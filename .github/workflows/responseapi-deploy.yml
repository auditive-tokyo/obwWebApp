name: Deploy ResponseAPI Functions

on:
  push:
    branches: [main, feature/*]
    paths:
      - 'lambda_functions/ResponseApi/**'
      - 'lambda_functions/SummarizeInquiry/**'
      - '.github/workflows/responseapi-deploy.yml'
      - 'template-responseapi-function.yaml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout main repository
        uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@8df5847569e6427dd6c4fb1cf565c83acfa8afa7 # v6
        with:
          role-to-assume: ${{ secrets.OIDC_ROLE_ARN }}
          aws-region: ap-northeast-1

      - name: Set up AWS SAM CLI
        uses: aws-actions/setup-sam@d78e1a4a9656d3b223e59b80676a797f20093133 # v2
        with:
          use-installer: true

      - name: SAM Build
        run: sam build --template-file template-responseapi-function.yaml

      - name: SAM Deploy
        env:
          OPENAI_API_KEY_FROM_SECRET: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_VECTOR_STORE_ID_FACILITY_FROM_SECRET: ${{ secrets.OPENAI_VECTOR_STORE_ID_FACILITY }}
          OPENAI_VECTOR_STORE_ID_TRANSPORT_FROM_SECRET: ${{ secrets.OPENAI_VECTOR_STORE_ID_TRANSPORT }}
          TELEGRAM_BOT_TOKEN_FROM_SECRET: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID_FROM_SECRET: ${{ secrets.TELEGRAM_CHAT_ID }}
          CLOUDFRONT_SECRET_FROM_SECRET: ${{ secrets.CLOUDFRONT_SECRET }}
        run: |
          sam deploy \
            --template-file .aws-sam/build/template.yaml \
            --stack-name obw-responseapi-function \
            --capabilities CAPABILITY_IAM \
            --resolve-s3 \
            --no-confirm-changeset \
            --parameter-overrides \
              OpenAiApiKey="$OPENAI_API_KEY_FROM_SECRET" \
              OpenAiVectorStoreIdFacility="$OPENAI_VECTOR_STORE_ID_FACILITY_FROM_SECRET" \
              OpenAiVectorStoreIdTransport="$OPENAI_VECTOR_STORE_ID_TRANSPORT_FROM_SECRET" \
              TelegramBotToken="$TELEGRAM_BOT_TOKEN_FROM_SECRET" \
              TelegramChatId="$TELEGRAM_CHAT_ID_FROM_SECRET" \
              CloudFrontSecret="$CLOUDFRONT_SECRET_FROM_SECRET" \
            --no-fail-on-empty-changeset
